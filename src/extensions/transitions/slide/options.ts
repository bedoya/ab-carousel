import { ABTransitionOptions } from '@/interfaces';

/**
 * Default configuration for the Slide transition.
 */
export const slideTransitionDefaults: Partial<ABTransitionOptions> = {
    duration: 1000,
    direction: true,
};
